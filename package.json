{
  "name": "portfolio-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd portfolio && npm run dev",
    "build": "cd portfolio && npm run build",
    "start": "cd portfolio && npm run start",
    "lint": "cd portfolio && npm run lint",
    "install-deps": "cd portfolio && npm install --legacy-peer-deps"
  },
  "description": "Portfolio workspace with Next.js project in portfolio subdirectory"
}